<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Orders - SecureShop</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    {{> header}}

    <div class="container mt-4">
        <h2>My Orders</h2>

        {{#if orders}}
        <div class="row">
            {{#each orders}}
            <div class="col-md-12 mb-3">
                <div class="card">
                    <div class="card-body">
                        <div class="row align-items-center">
                            <div class="col-md-2">
                                <h5 class="mb-0">Order #{{id}}</h5>
                            </div>
                            <div class="col-md-2">
                                <span class="badge {{#if (eq status 'pending')}}bg-warning{{else if (eq status 'processing')}}bg-info{{else if (eq status 'shipped')}}bg-primary{{else if (eq status 'delivered')}}bg-success{{else}}bg-danger{{/if}}">
                                    {{status}}
                                </span>
                            </div>
                            <div class="col-md-2">
                                <small class="text-muted">{{created_at}}</small>
                            </div>
                            <div class="col-md-2">
                                <strong>${{total_amount}}</strong>
                            </div>
                            <div class="col-md-2">
                                <small>{{items.length}} item{{#if (gt items.length 1)}}s{{/if}}</small>
                            </div>
                            <div class="col-md-2">
                                <a href="/orders/{{id}}" class="btn btn-outline-primary btn-sm">View Details</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>

        <!-- Pagination (simplified) -->
        {{#if (gt currentPage 1)}}
        <nav aria-label="Order pagination">
            <ul class="pagination justify-content-center">
                <li class="page-item">
                    <a class="page-link" href="/orders?page={{subtract currentPage 1}}">Previous</a>
                </li>
                <li class="page-item active">
                    <span class="page-link">{{currentPage}}</span>
                </li>
                <li class="page-item">
                    <a class="page-link" href="/orders?page={{add currentPage 1}}">Next</a>
                </li>
            </ul>
        </nav>
        {{/if}}
        {{else}}
        <div class="text-center mt-5">
            <h4>No orders yet</h4>
            <p class="text-muted">Your order history will appear here once you make a purchase.</p>
            <a href="/products" class="btn btn-primary">Start Shopping</a>
        </div>
        {{/if}}
    </div>

    {{> footer}}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

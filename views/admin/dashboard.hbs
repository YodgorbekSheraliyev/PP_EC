<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - SecureShop</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    {{> header}}

    <div class="container mt-4">
        <h2>Admin Dashboard</h2>

        <div class="row mb-4">
            <div class="col-md-3">
                <div class="card text-white bg-primary">
                    <div class="card-body">
                        <h5 class="card-title">{{stats.users}}</h5>
                        <p class="card-text">Total Users</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card text-white bg-success">
                    <div class="card-body">
                        <h5 class="card-title">{{stats.products}}</h5>
                        <p class="card-text">Total Products</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card text-white bg-info">
                    <div class="card-body">
                        <h5 class="card-title">{{stats.orders}}</h5>
                        <p class="card-text">Total Orders</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card text-white bg-warning">
                    <div class="card-body">
                        <h5 class="card-title">${{stats.revenue}}</h5>
                        <p class="card-text">Total Revenue</p>
                    </div>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header">
                        <h5>Quick Actions</h5>
                    </div>
                    <div class="card-body">
                        <div class="d-grid gap-2">
                            <a href="/products/admin/new" class="btn btn-success">Add New Product</a>
                            <a href="/products/admin" class="btn btn-primary">Manage Products</a>
                            <a href="/admin/users" class="btn btn-secondary">Manage Users</a>
                            <a href="/orders/admin/all" class="btn btn-info">View All Orders</a>
                            <a href="/admin/analytics" class="btn btn-warning">View Analytics</a>
                            <a href="/admin/logs" class="btn btn-dark">View Logs</a>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header">
                        <h5>Recent Orders</h5>
                    </div>
                    <div class="card-body">
                        {{#if recentOrders}}
                        {{#each recentOrders}}
                        <div class="mb-3 pb-3 border-bottom">
                            <div class="d-flex justify-content-between">
                                <strong>Order #{{id}}</strong>
                                <span class="badge {{#if (eq status 'pending')}}bg-warning{{else if (eq status 'processing')}}bg-info{{else if (eq status 'shipped')}}bg-primary{{else if (eq status 'delivered')}}bg-success{{else}}bg-danger{{/if}}">{{status}}</span>
                            </div>
                            <small class="text-muted">{{username}} - {{created_at}}</small>
                            <div class="mt-1">
                                <strong>${{total_amount}}</strong>
                                <a href="/orders/admin/all" class="btn btn-sm btn-outline-primary float-end">View All</a>
                            </div>
                        </div>
                        {{/each}}
                        {{else}}
                        <p class="text-muted">No recent orders</p>
                        {{/if}}
                    </div>
                </div>
            </div>
        </div>
    </div>

    {{> footer}}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

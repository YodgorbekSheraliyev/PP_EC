<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Security Logs - SecureShop Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    {{> header}}

    <div class="container mt-4">
        <h2>Security Logs</h2>
        <p class="text-muted">Monitor security events and user activities</p>

        {{#if logs}}
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Timestamp</th>
                        <th>Action</th>
                        <th>User</th>
                        <th>IP Address</th>
                        <th>Details</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each logs}}
                    <tr>
                        <td>{{timestamp}}</td>
                        <td>
                            <span class="badge {{#if (eq action "Failed login attempt")}}bg-danger{{else}}bg-info{{/if}}">
                                {{action}}
                            </span>
                        </td>
                        <td>{{user}}</td>
                        <td>{{ip}}</td>
                        <td>{{details}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
        {{else}}
        <div class="alert alert-info">
            No security logs available.
        </div>
        {{/if}}

        <div class="mt-4">
            <h5>Log Retention</h5>
            <p>Security logs are retained for 90 days. For longer retention or detailed analysis, contact system administrator.</p>
        </div>
    </div>

    {{> footer}}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Products - SecureShop Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    {{> header}}

    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2>Manage Products</h2>
            <a href="/products/admin/new" class="btn btn-success">Add New Product</a>
        </div>

        {{#if products}}
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Category</th>
                        <th>Price</th>
                        <th>Stock</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each products}}
                    <tr>
                        <td>{{id}}</td>
                        <td>{{name}}</td>
                        <td>{{category}}</td>
                        <td>${{price}}</td>
                        <td>
                            {{#if (gt stock_quantity 10)}}
                                <span class="badge bg-success">{{stock_quantity}}</span>
                            {{else if (gt stock_quantity 0)}}
                                <span class="badge bg-warning">{{stock_quantity}}</span>
                            {{else}}
                                <span class="badge bg-danger">{{stock_quantity}}</span>
                            {{/if}}
                        </td>
                        <td>
                            <a href="/products/admin/{{id}}/edit" class="btn btn-sm btn-primary">Edit</a>
                            <form method="POST" action="/products/admin/{{id}}/delete" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this product?')">
                                <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                            </form>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
        {{else}}
        <div class="alert alert-info">
            No products found. <a href="/products/admin/new">Add your first product</a>.
        </div>
        {{/if}}
    </div>

    {{> footer}}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
